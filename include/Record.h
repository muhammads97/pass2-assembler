#ifndef RECORD_H
#define RECORD_H
#include<bits/stdc++.h>
using namespace std;
class Record
{
    public:
        Record();
        int startAdd;
        string obSize = "",objRec = "";
        virtual ~Record();

    protected:

    private:
};

#endif // RECORD_H

#ifndef READFILE_H
#define READFILE_H
#include<bits/stdc++.h>

class ReadFile
{
    public:
        ReadFile();
        std::vector<std::string> read(std::string path);

    protected:

    private:

};

#endif // READFILE_H
